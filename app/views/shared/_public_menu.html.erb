  <div class="navbar">
    <div class="navbar-inner">
      <%= link_to "Ozwood", root_url, :class => "brand" %>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <% if  @current_tab == :index %>
            <li class="active"><%= link_to "Home", root_url %></li>
          <% else %>
            <li class=""><%= link_to "Home", root_url %></li>
          <% end %>

          <% if  @current_tab == :floorings %>
            <li class="active"><%= link_to "Products", floorings_url %></li>
          <% else %>
            <li class=""><%= link_to "Products", floorings_url %></li>
          <% end %>

          <% if  @current_tab == :shops %>
            <li class="active"><%= link_to "Shops", shops_path %></li>
          <% else %>
            <li class=""><%= link_to "Shops", shops_path %></li>
          <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div><!-- /.navbar-inner -->
  </div><!-- /.navbar -->